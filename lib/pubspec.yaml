name: flutter2w_project
description: A simple Dart project
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

#dependencies:
#  mysql1: ^0.20.0
  # Add packages here if needed


dependencies:
  flutter:
    sdk: flutter
  mysql_client: ^0.0.27  # Replace mysql1 with this